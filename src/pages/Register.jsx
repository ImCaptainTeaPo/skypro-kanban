import { RegisterWrapper, RegisterTitle, RegisterButton } from './Register.styled';

function Register() {
  return (
    <RegisterWrapper>
      <RegisterTitle>Register Page</RegisterTitle>
      <RegisterButton>Submit</RegisterButton>
    </RegisterWrapper>
  );
}

export default Register;
